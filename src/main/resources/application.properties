server.port=8080
spring.datasource.url=jdbc:mysql://localhost:3306/BlogAppApi
spring.datasource.username=root
spring.datasource.password=root@123

# --- NEW CLOUD CONFIG (Use Environment Variables) ---
# We use ${VAR_NAME:default_value} syntax.
# If DB_URL is found in environment, it uses it. If not, it falls back to localhost (good for testing).

#spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/BlogAppApi?sslMode=REQUIRED}
#spring.datasource.username=${DB_USER:root}
#spring.datasource.password=${DB_PASS:root@123}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

logging.level.org.springframework.security=DEBUG

spring.security.user.name=koushubh
spring.security.user.password=123456
spring.security.user.roles=ADMIN



